# Notion — doc-upsert skill (optional; only if you sync docs to Notion)
# Create a copy named `.env` at the repo root (never commit `.env`).
#
# NOTION_TOKEN=secret_...   # Internal integration secret from Notion
# NOTION_DATABASE_ID=32aeef158f0e8027a879c35c8fd65fe2
# NOTION_PROJECT=42hz-example-repo   # Optional; overrides Project when set
