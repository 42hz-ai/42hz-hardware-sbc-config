"""Greeting logic — keep commands thin."""

# Copier demo: third template revision (conflict exercise).


def build_greeting(name: str) -> str:
    """Return a greeting string for the given name."""
    return f"Template v3 says hi, {name}!"
